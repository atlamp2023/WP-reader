package com.github.atlamp2023.wpreader.core.util

//typealias updateState = (state: State) -> State

enum class State {
    LOCAL, REMOTE;
    //fun update( lambda: updateState ): State = lambda(this)
}